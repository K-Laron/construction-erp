# Implementation Plans — Construction Supply ERP Production Hardening

Generated by improve/audit skill on **2026-07-11**.  
**Planned against:** commit `cb7bb9b` (+ uncommitted working tree at plan time).  
**Repo:** `/home/enne/Projects/construction-erp`

> **Executor instructions (all plans):**  
> 1. Read the full plan file before editing anything.  
> 2. Run the **Drift check** first.  
> 3. Follow steps in order; run every **Verify** command.  
> 4. If a STOP condition triggers, stop and report — do not improvise.  
> 5. Update this table’s Status column when you finish or block.  
> 6. Do **not** push, force-push, or open a PR unless the human operator asks.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Close unauthenticated actions & spoofable IP rate-limits | P1 | S | — | DONE |
| 002 | Fail-closed G/L posting + correct Cash/Credit/Check debit matrix | P1 | M | — | DONE |
| 003 | FIFO invoice allocation on customer payments | P1 | M | 002 | DONE |
| 004 | Idempotent goods receipt (no double stock/AP) | P1 | S | — | DONE |
| 005 | Checkout hardening: stock pre-check, overpay clamp, AR/AP floors | P2 | S | 002 | DONE |
| 006 | Fix Z-reading vatable sales to match VAT Option A | P2 | S | — | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Dependency notes

- **001, 002, 004, 006** can run **in parallel** (different primary files; watch merge conflicts in tests).
- **003 depends on 002** so payment G/L patterns and fail-closed journal rules are stable before allocating invoices.
- **005 depends on 002** so overpay rejection is consistent with G/L balance rules (do not invent a second overpay policy).
- After all plans: run full `npm test`, `npx tsc --noEmit`, `npm run lint` once as a release gate.

## Recommended AI feed order

If feeding one plan at a time to an AI agent, use this sequence:

1. `plans/001-authz-and-ip-hardening.md`
2. `plans/002-gl-fail-closed-payment-matrix.md`
3. `plans/004-receive-goods-idempotency.md` (can run parallel with 001/002)
4. `plans/003-invoice-payment-allocation.md`
5. `plans/005-checkout-stock-and-balance-guards.md`
6. `plans/006-zreading-vat-math.md`

## Verification baseline (repo-wide)

| Purpose | Command | Expected |
|---------|---------|----------|
| Install | `npm install` | exit 0 |
| Tests | `npm test` | all suites pass (47+ after new tests) |
| Typecheck | `npx tsc --noEmit` | exit 0, no errors |
| Lint | `npm run lint` | exit 0 (currently 1 known prefer-const issue may exist; plans that touch `transactions.ts` should fix it if still present) |

## Findings covered

| Audit # | Finding | Plan |
|---------|---------|------|
| 1–2 | Silent G/L drop + broken Credit/Check debits | 002 |
| 3 | Payments don’t reduce invoice `balance_due` | 003 |
| 4–7 | `getUsers` / `lockStore` unauth; spoofable `providedIp`; XFF trust | 001 |
| 8 | Double `receiveGoods` | 004 |
| 9 | Z-reading vatable base wrong vs delivery VAT | 006 |
| 10–12 | Stock check, overpay, AR/AP floors | 005 |

## Findings deferred (not planned here)

- HMAC chain secondary sort key (`ORDER BY date, id`) — integrity polish; do after money paths.
- UUID vs seed item IDs (`item-blocks-4`) — data migration; separate plan if seed products must support returns.
- Automated nightly backup cron — ops; see `docs/operator/DEPLOYMENT.md` follow-up.
- PWA service-worker cache strategy — multi-cashier UX; separate plan.
- PRD modules not built (payroll, fixed assets, petty cash) — product direction, not bugfix.
- `swapDatabase` mid-flight race — residual accepted risk for admin-only restore.

## Commit message style (from repo history)

Use short conventional-ish subjects, e.g.:

- `security: require auth on getUsers and lockStore; ignore client IP`
- `fix: fail-closed G/L and correct credit/check cash debits`
- `fix: allocate customer payments to invoices FIFO`

Do not push unless instructed.
